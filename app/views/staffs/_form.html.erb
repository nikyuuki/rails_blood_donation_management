<%= form_with(model: staff) do |form| %>
  <% if staff.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(staff.errors.count, "error") %> prohibited this staff from being saved:</h2>

      <ul>
        <% staff.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


      <div class="field form-group">
        <%= form.label :name, "Staff Name" %>
        <%= form.text_field :name, placeholder: "Staff Name", class: "form-control" %>
      </div> <br>

    <div class="field form-group">
          <%= form.label :role, "Role" %><br>

          <div class="form-check">
            <%= form.radio_button :role, "Nurse", class: "form-check-input", id: "nurse" %>
            <%= form.label :role, "Nurse", class: "form-check-label", for: "nurse" %>
          </div>

          <div class="form-check">
            <%= form.radio_button :role, "Doctor", class: "form-check-input", id: "doctor" %>
            <%= form.label :role, "Doctor", class: "form-check-label", for: "doctor" %>
          </div>
        </div>


      <div>
        <%= form.submit%>
      </div>


<% end %>
